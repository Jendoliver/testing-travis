apply plugin: "java"
sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

sourceSets {
	main {
		java {
			srcDir 'src/main'
		}
	}
	test {
		java {
			srcDir 'src/test'
		}
	}
}

task testBranch1 {
	println 'esta NO toca'
	test {
		filter {
			includeTestsMatching "branch1tests.*"
		}
		testLogging {
			events "passed", "skipped", "failed"
			exceptionFormat "full"
		}
	}
}

task testBranch2 {
	println 'esta toca'
	test {
		filter {
			includeTestsMatching "branch2tests.*"
		}
		testLogging {
			events "passed", "skipped", "failed"
			exceptionFormat "full"
		}
	}
}